{
  "name": "@bulatlib/di-examples",
  "private": true,
  "scripts": {
    "reinstall-di": "bun rm @bulatlib/di && bun pm cache rm && bun add ../pack/bulatlib-di-0.0.4.tgz",
    "basic": "bun run ./src/basic.ts",
    "mock": "bun run ./src/mock.ts"
  },
  "type": "module",
  "dependencies": {
    "@bulatlib/di": "../pack/bulatlib-di-0.0.4.tgz"
  }
}
